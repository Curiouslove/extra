public class Calculator {
    /*
     * the parameters collected would help in passing
     * values into the method.
     * the operator parameter would ease the validation and throwing of exception
     */
    public static int addNumbers(int valueOne, String operator, int valueTwo) {
        return valueOne + valueTwo;
    }

}
